CREATE TABLE direction(
	id BIGINT(20) PRIMARY KEY AUTO_INCREMENT,
	principal VARCHAR(50) NOT NULL,
	code_budgetaire VARCHAR(50) NOT NULL,
	name_school VARCHAR(50) NOT NULL,
	number_school INT NOT NULL
);

